<h1><%= @task.title %></h1>
<h2><%= @task.details %></h2>

<%= link_to "Edit", edit_task_path(@task.id) %>
<%= link_to "See all tasks", tasks_path %>
<%= link_to "Delete", task_path(@task.id), data: { turbo_method: :delete, turbo_confirm: "Are you sure?"} %>
